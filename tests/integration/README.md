# Integration Tests

End-to-end parse pipeline tests should verify upload, parse-run completion, object paths, AST output, and frontend-inspectable bbox data once the local service stack is connected.
